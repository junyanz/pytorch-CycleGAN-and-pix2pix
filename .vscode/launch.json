{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Slurm GPU",
            "type": "debugpy",
            "request": "attach",
            "connect": {
                "host": "gpu01",
                "port": 5678
            },
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "${workspaceFolder}"
                }
            ],
            "justMyCode": false
        }
    ]
}